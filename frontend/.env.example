# VITE_API_URL: Base URL of the deployed backend API (GraphQL + REST).
# Leave empty for local dev — Vite proxies /graphql and /api to BACKEND_URL (see vite.config.ts).
# In production, set this to your backend's public URL, e.g. https://api.example.com
VITE_API_URL=
